
# Setting required libraries/modules
set(LIBRARIES
    HGToolsCore
    HGPackageProcessorLibrary
)

# Setting required additional includes
set(INCLUDES)

describe_tool(PackageProcessor "${LIBRARIES}" "${INCLUDES}")